<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/logo.png" type="image/png">
    <link rel="stylesheet" href="./css/main.css">
    <link rel="stylesheet" href="./css/main2.css">
    <link rel="stylesheet" href="./css/contact.css">
    <title>Contact</title>
</head>
<body class="bg-gradient-to-b from-yellow-600 via-red-900 to-black min-h-screen w-full text-white">
    <%- include('partials/header', { page: 'contact' }) %>

    <div class="contact-container p-6 max-w-3xl mx-auto bg-white bg-opacity-10 rounded-xl shadow-lg mt-10">
        <h1 class="text-3xl font-bold mb-4 text-center"><%= title %></h1>
        <p class="mb-2"><%= message %></p>
        <p>Email: <%= email %></p>
        <p>Phone: <%= phone %></p>
        <p>Address: <%= address %></p>

        <form action="/sendMessage" method="POST" class="mt-8 space-y-4">
            <div>
                <label for="name" class="block mb-1 font-semibold">Your Name</label>
                <input type="text" name="name" id="name" required class="w-full p-2 rounded bg-white text-black">
            </div>
            <div>
                <label for="email" class="block mb-1 font-semibold">Your Email</label>
                <input type="email" name="email" id="email" required class="w-full p-2 rounded bg-white text-black">
            </div>
            <div>
                <label for="subject" class="block mb-1 font-semibold">Subject</label>
                <input type="text" name="subject" id="subject" required class="w-full p-2 rounded bg-white text-black">
            </div>
            <div>
                <label for="message" class="block mb-1 font-semibold">Message</label>
                <textarea name="message" id="message" rows="5" required class="w-full p-2 rounded bg-white text-black"></textarea>
            </div>
            <button type="submit" class="btn bg-red-700 hover:bg-red-800 text-white font-bold px-6 py-2 rounded">
                Send Message
            </button>
        </form>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
